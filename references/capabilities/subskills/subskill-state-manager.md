# 子技能：state-manager

## Trigger

治理状态读写；Rule Capture 候选暂存于 `state.json` 时的读写。

## Authority

生成物 `state-manager` 子技能。

## Invoke

加载子技能；按模板操作 state / rule_capture 字段。

## Verify

状态变更可追踪；不发明平行状态家。

## Non-goals

不是 Discovery Ledger。
